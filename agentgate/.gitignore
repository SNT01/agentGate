# Runtime state — contains the registry root key and the broker signing key.
data/
data-demo/

# Local configuration and secrets
.env
*.pem
*.key

node_modules/
*.log

# Dashboard build output — generated by `cd ui && npm run build`.
# ui/package-lock.json IS committed; that lockfile is what npm ci reproduces
# this from.
src/ui/dist/
ui/node_modules/
ui/dist/
