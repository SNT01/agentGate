{
  "name": "agentgate-ui",
  "version": "1.0.0",
  "private": true,
  "description": "AgentGate admin dashboard. Ships as static assets served by the broker — the broker itself has zero runtime dependencies; these are build-time only.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.4.3",
    "@vitejs/plugin-react": "^4.3.4"
  }
}
